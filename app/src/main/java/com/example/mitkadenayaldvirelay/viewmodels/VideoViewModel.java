package com.example.mitkadenayaldvirelay.viewmodels;

import android.opengl.Visibility;

import androidx.annotation.NonNull;
import androidx.lifecycle.LiveData;
import androidx.lifecycle.ViewModel;

import com.example.mitkadenayaldvirelay.classes.Video;

import java.io.Closeable;
import java.util.List;

public class VideoViewModel extends ViewModel {

    /*
    private VideoRepository mRepository;

    private LiveData<List<Video>> videos;

    public VideoViewModel() {
        this.mRepository = new VideoRepository();
        this.videos = mRepository.getAll();
    }

     */

}
